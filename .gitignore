.DS_Store

# Build output from ./setup: docker save archives, .smolmachine packs.
# Regenerated by ./setup -- never worth committing (multi-GB).
/build/
*.tar
*.smolmachine
*.smolmachine.smolmachine

# Stray OCI export from an earlier experiment, kept locally but not tracked.
/rootfs/
